name = "pocketbase-cn"
compatibility_date = "2025-01-01"

# Cloudflare Pages will use this output directory for deployments.
pages_build_output_dir = "./dist"

# Session KV namespace - configure in Cloudflare dashboard when needed
# Temporarily disabled to fix build issue
# kv_namespaces = [
#   { binding = "SESSION", id = "placeholder-kv-id", preview_id = "placeholder-preview-kv-id" }
# ]

