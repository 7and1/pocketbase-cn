# PocketBase.cn backend (PocketBase)
#
# DO NOT COMMIT real secrets. Copy to `apps/backend/.env` and fill in production values.

# CORS (comma-separated)
PB_CORS_ORIGINS=https://pocketbase.cn,https://www.pocketbase.cn

# Security (Required in production)
PB_CSRF_SECRET=                     # 32+ character random string

# GitHub API (Optional, increases rate limit)
GITHUB_TOKEN=

# Email (Resend)
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@pocketbase.cn
NEWSLETTER_FROM="PocketBase.cn <noreply@pocketbase.cn>"

# Site URL for links in emails/webhooks
PUBLIC_SITE_URL=https://pocketbase.cn

# Optional webhook for release notifications (Discord/Slack style)
ALERT_WEBHOOK_URL=

# GitHub webhook token for `/api/webhooks/github/release`
GITHUB_WEBHOOK_TOKEN=

# Analytics (Optional)
UMAMI_WEBSITE_ID=
UMAMI_SRC=

# Error Tracking (Optional)
SENTRY_DSN=

# Litestream R2 backup configuration
LITESTREAM_BUCKET=pocketbase-backups
PB_S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
LITESTREAM_ACCESS_KEY_ID=
LITESTREAM_SECRET_ACCESS_KEY=

