# PocketBase.cn Staging Environment
# Copy this file to /opt/pocketbase-staging/.env and fill in the values

# CORS (comma-separated) - use staging URLs
PB_CORS_ORIGINS=https://staging.pocketbase.cn

# GitHub API token (optional, increases rate limit)
GITHUB_TOKEN=

# Resend (optional, used by newsletter confirm & release notifications)
# Use test API key for staging
RESEND_API_KEY=
NEWSLETTER_FROM="PocketBase.cn Staging <staging@pocketbase.cn>"

# Site URL for links in emails/webhooks
PUBLIC_SITE_URL=https://staging.pocketbase.cn

# Optional webhook for release notifications (Discord/Slack style)
# Use staging webhook channel
ALERT_WEBHOOK_URL=

# GitHub webhook token for `/api/webhooks/github/release`
GITHUB_WEBHOOK_TOKEN=

# Litestream R2 backup configuration (same as production but separate path)
LITESTREAM_BUCKET=pocketbase-staging-backups
PB_S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
LITESTREAM_ACCESS_KEY_ID=
LITESTREAM_SECRET_ACCESS_KEY=
